#include "tire_pressure_sensor.h"

// Executes a specified action for the tire pressure sensor.
void TirePressureSensor::doAction(string action)
{
    cout << "I sensor " << id << " do " << action << endl;
}